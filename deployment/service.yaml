apiVersion: v1
kind: Service
metadata:
  name: pipelinetestservice
spec:
  type: NodePort
  ports:
    - port: 8085
      targetPort: 8085
      protocol: TCP
      name: http
      nodePort: 31004
  selector:
    app: pipelinedeployment
      
